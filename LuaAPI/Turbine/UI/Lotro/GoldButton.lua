local GoldButton = {} 
--- Sets the background color of the control.  
--- (Inherited from Control) 
function GoldButton:SetBackColor() 
end 
--- Selects all the text in the control.  
--- (Inherited from Label) 
function GoldButton:SelectAll() 
end 
--- Converts a coordinate from control space to screen space.  
--- (Inherited from Control) 
function GoldButton:PointToScreen() 
end 
--- Sets the background image of the control.  
--- (Inherited from Control) 
function GoldButton:SetBackground() 
end 
--- Sets the blend mode applied to the background image.  
--- (Inherited from Control) 
function GoldButton:SetBlendMode() 
end 
--- Sets the blend mode applied to the background color.  
--- (Inherited from Control) 
function GoldButton:SetBackColorBlendMode() 
end 
--- Sets a flag indicating if the control is enabled.  
--- (Inherited from Control) 
function GoldButton:SetEnabled() 
end 
--- Gets the Z ordering index of the control.  
--- (Inherited from Control) 
function GoldButton:GetZOrder() 
end 
--- Gets the solid background color of the control.  
--- (Inherited from Control) 
function GoldButton:GetBackColor() 
end 
--- Sets the current font.  
--- (Inherited from Label) 
function GoldButton:SetFont() 
end 
--- Test if the shift key is pressed.  
--- (Inherited from Control) 
function GoldButton:IsShiftKeyDown() 
end 
--- Sets the font style.  
--- (Inherited from Label) 
function GoldButton:SetFontStyle() 
end 
--- Gets if the label is set to display multiple lines.  
--- (Inherited from Label) 
function GoldButton:IsMultiline() 
end 
--- Sets the foreground color.  
--- (Inherited from Label) 
function GoldButton:SetForeColor() 
end 
--- Gets a flag indicating if markup is enabled for the label.  
--- (Inherited from Label) 
function GoldButton:IsMarkupEnabled() 
end 
--- Sets the height of the control.  
--- (Inherited from Control) 
function GoldButton:SetHeight() 
end 
--- Test if the control key is pressed.  
--- (Inherited from Control) 
function GoldButton:IsControlKeyDown() 
end 
--- Sets the horizontal scroll bar.  
--- (Inherited from ScrollableControl) 
function GoldButton:SetHorizontalScrollBar() 
end 
--- Gets the background graphic of the control.  
--- (Inherited from Control) 
function GoldButton:GetBackground() 
end 
--- Gets the blend mode applied to the background color.  
--- (Inherited from Control) 
function GoldButton:GetBackColorBlendMode() 
end 
--- Sets the left coordinate of the window.  
--- (Inherited from Control) 
function GoldButton:SetLeft() 
end 
--- Gets if the control supports drop operations from drag and drop.  
--- (Inherited from Control) 
function GoldButton:GetAllowDrop() 
end 
--- Request that the control take focus.  
--- (Inherited from Control) 
function GoldButton:Focus() 
end 
--- Sets a flag indicating if markup is enabled for the label.  
--- (Inherited from Label) 
function GoldButton:SetMarkupEnabled() 
end 
--- Gets a flag indicating if the mouse will see this control.  
--- (Inherited from Control) 
function GoldButton:SetMouseVisible() 
end 
--- Gets the length of the selected text.  
--- (Inherited from Label) 
function GoldButton:GetSelectionLength() 
end 
--- Gets a flag indicating if the control wants to receive key events.  
--- (Inherited from Control) 
function GoldButton:GetWantsKeyEvents() 
end 
--- Sets if the label should display on multiple lines.  
--- (Inherited from Label) 
function GoldButton:SetMultiline() 
end 
--- Gets the parent of the control.  
--- (Inherited from Control) 
function GoldButton:GetParent() 
end 
--- Gets the alignment of the text within the label.  
--- (Inherited from Label) 
function GoldButton:GetTextAlignment() 
end 
--- Sets the opacity of the window.  
--- (Inherited from Control) 
function GoldButton:SetOpacity() 
end 
--- Gets the opacity of the window.  
--- (Inherited from Control) 
function GoldButton:GetOpacity() 
end 
--- Test if the alt key is pressed.  
--- (Inherited from Control) 
function GoldButton:IsAltKeyDown() 
end 
--- Sets the text outline color.  
--- (Inherited from Label) 
function GoldButton:SetOutlineColor() 
end 
--- Gets the left coordinate of the control.  
--- (Inherited from Control) 
function GoldButton:GetLeft() 
end 
--- Gets the width of the control.  
--- (Inherited from Control) 
function GoldButton:GetWidth() 
end 
--- Sets the parent of the control.  
--- (Inherited from Control) 
function GoldButton:SetParent() 
end 
--- Gets the height of the control.  
--- (Inherited from Control) 
function GoldButton:GetHeight() 
end 
--- Gets the top coordinate of the window.  
--- (Inherited from Control) 
function GoldButton:GetTop() 
end 
--- Appends text to the end of the label.  
--- (Inherited from Label) 
function GoldButton:AppendText() 
end 
--- Sets the position of the control.  
--- (Inherited from Control) 
function GoldButton:SetPosition() 
end 
--- Gets the text of the label.  
--- (Inherited from Label) 
function GoldButton:GetText() 
end 
--- Sets a flag indicating if the text in the label can be selected.  
--- (Inherited from Label) 
function GoldButton:SetSelectable() 
end 
--- Replaces the selected text.  
--- (Inherited from Label) 
function GoldButton:SetSelectedText() 
end 
--- Gets the blend mode applied to the background image.  
--- (Inherited from Control) 
function GoldButton:GetBlendMode() 
end 
--- Sets the selected text.  
--- (Inherited from Label) 
function GoldButton:SetSelection() 
end 
--- Gets the current font.  
--- (Inherited from Label) 
function GoldButton:GetFont() 
end 
--- Sets the length of the selected text.  
--- (Inherited from Label) 
function GoldButton:SetSelectionLength() 
end 
--- Sets the position of the selection start.  
--- (Inherited from Label) 
function GoldButton:SetSelectionStart() 
end 
--- Gets the position of the selection start.  
--- (Inherited from Label) 
function GoldButton:GetSelectionStart() 
end 
--- Sets the size of the control.  
--- (Inherited from Control) 
function GoldButton:SetSize() 
end 
--- Gets the foreground color.  
--- (Inherited from Label) 
function GoldButton:GetForeColor() 
end 
--- Sets the text of the label.  
--- (Inherited from Label) 
function GoldButton:SetText() 
end 
--- Gets the list of child controls.  
--- (Inherited from Control) 
function GoldButton:GetControls() 
end 
--- Sets the alignment of the text within the label.  
--- (Inherited from Label) 
function GoldButton:SetTextAlignment() 
end 
--- Sets the top coordinate of the window.  
--- (Inherited from Control) 
function GoldButton:SetTop() 
end 
--- Gets a flag indicating if the control is visible.  
--- (Inherited from Control) 
function GoldButton:IsVisible() 
end 
--- Sets if the control supports drop operations from drag and drop.  
--- (Inherited from Control) 
function GoldButton:SetAllowDrop() 
end 
--- Sets the vertical scroll bar.  
--- (Inherited from ScrollableControl) 
function GoldButton:SetVerticalScrollBar() 
end 
--- Gets a flag indicating if the text in the label is selectable.  
--- (Inherited from Label) 
function GoldButton:IsSelectable() 
end 
--- Deselects all the text in the control.  
--- (Inherited from Label) 
function GoldButton:DeselectAll() 
end 
--- Sets the visible flag of a control.  
--- (Inherited from Control) 
function GoldButton:SetVisible() 
end 
--- Gets a flag indicating if the mouse will see this control.  
--- (Inherited from Control) 
function GoldButton:IsMouseVisible() 
end 
--- Sets a flag indicating if the control wants to receive key events.  
--- (Inherited from Control) 
function GoldButton:SetWantsKeyEvents() 
end 
--- Gets a flag indicating if the control is displayed.  
--- (Inherited from Control) 
function GoldButton:IsDisplayed() 
end 
--- Sets the flag indicating if the cotnrol wants to receive update notifications.  
--- (Inherited from Control) 
function GoldButton:SetWantsUpdates() 
end 
--- Returns true if the control has focus.  
--- (Inherited from Control) 
function GoldButton:HasFocus() 
end 
--- Sets the width of the control.  
--- (Inherited from Control) 
function GoldButton:SetWidth() 
end 
--- Sets the Z order of the control.  
--- (Inherited from Control) 
function GoldButton:SetZOrder() 
end 
--- Gets the selected text of the label.  
--- (Inherited from Label) 
function GoldButton:GetSelectedText() 
end 
--- Gets the position of the control.  
--- (Inherited from Control) 
function GoldButton:GetPosition() 
end 
--- Gets the vertical scroll bar.  
--- (Inherited from ScrollableControl) 
function GoldButton:GetVerticalScrollBar() 
end 
--- Gets the text outline color.  
--- (Inherited from Label) 
function GoldButton:GetOutlineColor() 
end 
--- Gets a flag indicating if the control is enabled.  
--- (Inherited from Control) 
function GoldButton:IsEnabled() 
end 
--- Gets the mouse position relative to this control.  
--- (Inherited from Control) 
function GoldButton:GetMousePosition() 
end 
--- Inserts text into the label at the current cursor position.  
--- (Inherited from Label) 
function GoldButton:InsertText() 
end 
--- Gets the horizontal scroll bar.  
--- (Inherited from ScrollableControl) 
function GoldButton:GetHorizontalScrollBar() 
end 
--- Gets the flag indicating if the control wants to receive Update notifications.  
--- (Inherited from Control) 
function GoldButton:GetWantsUpdates() 
end 
--- Gets the length of the text of the label.  
--- (Inherited from Label) 
function GoldButton:GetTextLength() 
end 
--- Gets the size of the control.  
--- (Inherited from Control) 
function GoldButton:GetSize() 
end 
--- Gets the font style.  
--- (Inherited from Label) 
function GoldButton:GetFontStyle() 
end 
--- Converts a coordinate from control space to screen space.  
--- (Inherited from Control) 
function GoldButton:PointToClient() 
end 
--- Event fired when the mouse moves.  
--- (Inherited from Control) 
function GoldButton:MouseMove() 
end 
--- Event fired when the mouse leaves the cotnrol.  
--- (Inherited from Control) 
function GoldButton:MouseLeave() 
end 
--- Event fired when the mouse is hovering over the control.  
--- (Inherited from Control) 
function GoldButton:MouseHover() 
end 
--- Event fired when the mouse enters the control.  
--- (Inherited from Control) 
function GoldButton:MouseEnter() 
end 
--- Event fired when a mouse button is pressed.  
--- (Inherited from Control) 
function GoldButton:MouseDown() 
end 
--- Event fired when a mouse button is double clicked.  
--- (Inherited from Control) 
function GoldButton:MouseDoubleClick() 
end 
--- Event fired when a mouse button is clicked.  
--- (Inherited from Control) 
function GoldButton:MouseClick() 
end 
--- Event fired when the position of the control changes.  
--- (Inherited from Control) 
function GoldButton:PositionChanged() 
end 
--- Event fired when the size of the control changes.  
--- (Inherited from Control) 
function GoldButton:SizeChanged() 
end 
--- Event fired when the visible state of the control changes.  
--- (Inherited from Control) 
function GoldButton:VisibleChanged() 
end 
--- Event fired when a mouse button is released.  
--- (Inherited from Control) 
function GoldButton:MouseUp() 
end 
--- Event fired when a key is released.  
--- (Inherited from Control) 
function GoldButton:KeyUp() 
end 
--- Event fired when a key is pressed down.  
--- (Inherited from Control) 
function GoldButton:KeyDown() 
end 
--- Event fired when the control loses focus.  
--- (Inherited from Control) 
function GoldButton:FocusLost() 
end 
--- Event fired when the control gains focus.  
--- (Inherited from Control) 
function GoldButton:FocusGained() 
end 
--- Event fired when the enabled state of the control changes.  
--- (Inherited from Control) 
function GoldButton:EnabledChanged() 
end 
--- Event fired when a drag drop operation enters the control.  
--- (Inherited from Control) 
function GoldButton:DragEnter() 
end 
--- Event fired when a drag drop operation starts the control.  
--- (Inherited from Control) 
function GoldButton:DragStart() 
end 
--- Event fired when a drag drop operation is completed.  
--- (Inherited from Control) 
function GoldButton:DragDrop() 
end 
--- Event fired when the user clicks on the button.  
--- (Inherited from Button) 
function GoldButton:Click() 
end 
--- Event fired every frame when WantsUpdates is enabled.  
--- (Inherited from Control) 
function GoldButton:Update() 
end 
--- Event fired when the enabled state of the control changes.  
--- (Inherited from Button) 
function GoldButton:EnabledChanged() 
end 
--- Event fired when a drag drop operation leaves the control.  
--- (Inherited from Control) 
function GoldButton:DragLeave() 
end 
--- Event fired when a mouse wheel moves.  
--- (Inherited from Control) 
function GoldButton:MouseWheel() 
end 
return GoldButton