local ItemInfoControl = {} 
--- Gets the size of the control.  
--- (Inherited from Control) 
function ItemInfoControl:GetSize() 
end 
--- Sets the flag indicating if the cotnrol wants to receive update notifications.  
--- (Inherited from Control) 
function ItemInfoControl:SetWantsUpdates() 
end 
--- Sets the top coordinate of the window.  
--- (Inherited from Control) 
function ItemInfoControl:SetTop() 
end 
--- Sets a flag indicating if the control wants to receive key events.  
--- (Inherited from Control) 
function ItemInfoControl:SetWantsKeyEvents() 
end 
--- Sets the opacity of the window.  
--- (Inherited from Control) 
function ItemInfoControl:SetOpacity() 
end 
--- Gets a flag indicating if the mouse will see this control.  
--- (Inherited from Control) 
function ItemInfoControl:IsMouseVisible() 
end 
--- Gets a flag indicating if the control is displayed.  
--- (Inherited from Control) 
function ItemInfoControl:IsDisplayed() 
end 
--- Sets the left coordinate of the window.  
--- (Inherited from Control) 
function ItemInfoControl:SetLeft() 
end 
--- Sets the item displayed by the control. 
function ItemInfoControl:SetItemInfo() 
end 
--- Sets the height of the control.  
--- (Inherited from Control) 
function ItemInfoControl:SetHeight() 
end 
--- Sets a flag indicating if the control is enabled.  
--- (Inherited from Control) 
function ItemInfoControl:SetEnabled() 
end 
--- Sets the blend mode applied to the background image.  
--- (Inherited from Control) 
function ItemInfoControl:SetBlendMode() 
end 
--- Gets the top coordinate of the window.  
--- (Inherited from Control) 
function ItemInfoControl:GetTop() 
end 
--- Sets the background image of the control.  
--- (Inherited from Control) 
function ItemInfoControl:SetBackground() 
end 
--- Sets the blend mode applied to the background color.  
--- (Inherited from Control) 
function ItemInfoControl:SetBackColorBlendMode() 
end 
--- Gets a flag indicating if the control wants to receive key events.  
--- (Inherited from Control) 
function ItemInfoControl:GetWantsKeyEvents() 
end 
--- Test if the control key is pressed.  
--- (Inherited from Control) 
function ItemInfoControl:IsControlKeyDown() 
end 
--- Gets the quantity displayed. 
function ItemInfoControl:GetQuantity() 
end 
--- Gets the parent of the control.  
--- (Inherited from Control) 
function ItemInfoControl:GetParent() 
end 
--- Gets the position of the control.  
--- (Inherited from Control) 
function ItemInfoControl:GetPosition() 
end 
--- Returns true if the control has focus.  
--- (Inherited from Control) 
function ItemInfoControl:HasFocus() 
end 
--- Sets the parent of the control.  
--- (Inherited from Control) 
function ItemInfoControl:SetParent() 
end 
--- Gets the opacity of the window.  
--- (Inherited from Control) 
function ItemInfoControl:GetOpacity() 
end 
--- Sets the background color of the control.  
--- (Inherited from Control) 
function ItemInfoControl:SetBackColor() 
end 
--- Gets the mouse position relative to this control.  
--- (Inherited from Control) 
function ItemInfoControl:GetMousePosition() 
end 
--- Sets if the control supports drop operations from drag and drop.  
--- (Inherited from Control) 
function ItemInfoControl:SetAllowDrop() 
end 
--- Gets the left coordinate of the control.  
--- (Inherited from Control) 
function ItemInfoControl:GetLeft() 
end 
--- Gets a flag indicating if the control is visible.  
--- (Inherited from Control) 
function ItemInfoControl:IsVisible() 
end 
--- Test if the shift key is pressed.  
--- (Inherited from Control) 
function ItemInfoControl:IsShiftKeyDown() 
end 
--- Converts a coordinate from control space to screen space.  
--- (Inherited from Control) 
function ItemInfoControl:PointToClient() 
end 
--- Gets the item displayed by the control. 
function ItemInfoControl:GetItemInfo() 
end 
--- Gets the width of the control.  
--- (Inherited from Control) 
function ItemInfoControl:GetWidth() 
end 
--- Test if the alt key is pressed.  
--- (Inherited from Control) 
function ItemInfoControl:IsAltKeyDown() 
end 
--- Gets the height of the control.  
--- (Inherited from Control) 
function ItemInfoControl:GetHeight() 
end 
--- Gets the flag indicating if the control wants to receive Update notifications.  
--- (Inherited from Control) 
function ItemInfoControl:GetWantsUpdates() 
end 
--- Sets the Z order of the control.  
--- (Inherited from Control) 
function ItemInfoControl:SetZOrder() 
end 
--- Gets the list of child controls.  
--- (Inherited from Control) 
function ItemInfoControl:GetControls() 
end 
--- Gets the blend mode applied to the background image.  
--- (Inherited from Control) 
function ItemInfoControl:GetBlendMode() 
end 
--- Gets a flag indicating if the control is enabled.  
--- (Inherited from Control) 
function ItemInfoControl:IsEnabled() 
end 
--- Gets if the control supports drop operations from drag and drop.  
--- (Inherited from Control) 
function ItemInfoControl:GetAllowDrop() 
end 
--- Gets the background graphic of the control.  
--- (Inherited from Control) 
function ItemInfoControl:GetBackground() 
end 
--- Gets the blend mode applied to the background color.  
--- (Inherited from Control) 
function ItemInfoControl:GetBackColorBlendMode() 
end 
--- Sets the size of the control.  
--- (Inherited from Control) 
function ItemInfoControl:SetSize() 
end 
--- Gets the solid background color of the control.  
--- (Inherited from Control) 
function ItemInfoControl:GetBackColor() 
end 
--- Gets the quantity displayed. 
function ItemInfoControl:SetQuantity() 
end 
--- Sets the position of the control.  
--- (Inherited from Control) 
function ItemInfoControl:SetPosition() 
end 
--- Request that the control take focus.  
--- (Inherited from Control) 
function ItemInfoControl:Focus() 
end 
--- Sets the visible flag of a control.  
--- (Inherited from Control) 
function ItemInfoControl:SetVisible() 
end 
--- Gets a flag indicating if the mouse will see this control.  
--- (Inherited from Control) 
function ItemInfoControl:SetMouseVisible() 
end 
--- Sets the width of the control.  
--- (Inherited from Control) 
function ItemInfoControl:SetWidth() 
end 
--- Gets the Z ordering index of the control.  
--- (Inherited from Control) 
function ItemInfoControl:GetZOrder() 
end 
--- Converts a coordinate from control space to screen space.  
--- (Inherited from Control) 
function ItemInfoControl:PointToScreen() 
end 
--- Event fired when the size of the control changes.  
--- (Inherited from Control) 
function ItemInfoControl:SizeChanged() 
end 
--- Event fired when a key is pressed down.  
--- (Inherited from Control) 
function ItemInfoControl:KeyDown() 
end 
--- Event fired when the mouse moves.  
--- (Inherited from Control) 
function ItemInfoControl:MouseMove() 
end 
--- Event fired when the mouse is hovering over the control.  
--- (Inherited from Control) 
function ItemInfoControl:MouseHover() 
end 
--- Event fired when a mouse button is pressed.  
--- (Inherited from Control) 
function ItemInfoControl:MouseDown() 
end 
--- Event fired when the mouse leaves the cotnrol.  
--- (Inherited from Control) 
function ItemInfoControl:MouseLeave() 
end 
--- Event fired when the enabled state of the control changes.  
--- (Inherited from Control) 
function ItemInfoControl:EnabledChanged() 
end 
--- Event fired when a mouse button is clicked.  
--- (Inherited from Control) 
function ItemInfoControl:MouseClick() 
end 
--- Event fired when a mouse wheel moves.  
--- (Inherited from Control) 
function ItemInfoControl:MouseWheel() 
end 
--- Event fired when the visible state of the control changes.  
--- (Inherited from Control) 
function ItemInfoControl:VisibleChanged() 
end 
--- Event fired when a key is released.  
--- (Inherited from Control) 
function ItemInfoControl:KeyUp() 
end 
--- Event fired when a drag drop operation is completed.  
--- (Inherited from Control) 
function ItemInfoControl:DragDrop() 
end 
--- Event fired when a mouse button is double clicked.  
--- (Inherited from Control) 
function ItemInfoControl:MouseDoubleClick() 
end 
--- Event fired when the control loses focus.  
--- (Inherited from Control) 
function ItemInfoControl:FocusLost() 
end 
--- Event fired when the mouse enters the control.  
--- (Inherited from Control) 
function ItemInfoControl:MouseEnter() 
end 
--- Event fired when a mouse button is released.  
--- (Inherited from Control) 
function ItemInfoControl:MouseUp() 
end 
--- Event fired when the control gains focus.  
--- (Inherited from Control) 
function ItemInfoControl:FocusGained() 
end 
--- Event fired when the enabled state of the control changes. 
function ItemInfoControl:EnabledChanged() 
end 
--- Event fired when a drag drop operation starts the control.  
--- (Inherited from Control) 
function ItemInfoControl:DragStart() 
end 
--- Event fired every frame when WantsUpdates is enabled.  
--- (Inherited from Control) 
function ItemInfoControl:Update() 
end 
--- Event fired when a drag drop operation enters the control.  
--- (Inherited from Control) 
function ItemInfoControl:DragEnter() 
end 
--- Event fired when a drag drop operation leaves the control.  
--- (Inherited from Control) 
function ItemInfoControl:DragLeave() 
end 
--- Event fired when the position of the control changes.  
--- (Inherited from Control) 
function ItemInfoControl:PositionChanged() 
end 
return ItemInfoControl